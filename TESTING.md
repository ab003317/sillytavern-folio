# 測試記錄

## 0.8.9：真實長聊天的向量與正文選頁

- 對一份已有 Folio 摘要的真實長聊天做只讀審計：舊版在新瀏覽器可產生查詢向量，但頁向量不在該瀏覽器的 IndexedDB，候選因此全靠 BM25，沒有語意分數。配置中的提取模型實際收到候選小摘要並合法返回空 `ids`，所以沒有召回舊正文；不是「小模型沒有被呼叫」。公開記錄不包含聊天內容、規模或連線名稱。
- 改為打開聊天時先讀快取，缺頁即用隨插件內建的向量模型從已有摘要自動重建；不呼叫總結／提取 API、不改摘要、不保存聊天。生成攔截器等待本次補齊完成後才排名，避免剛換瀏覽器時無提示退化成純文字檢索。失敗退避 60 秒後自動重試，手動補齊按鈕保留。
- 新摘要預設改為 180–350 字的檢索目錄卡，明確保留人物／別名／身份、地點與物件、行動因果、關係狀態、目標與未決線索，並禁止用空泛大意取代實體與事件。允許提取模型合法返回空，沒有加入強制亂選正文的備援。舊摘要不會自動花費模型請求改寫，須由玩家明確執行「一鍵重新整理全部」後才套用新格式。
- 部署後以同一份真實長聊天及全新瀏覽器重驗：全部已完成摘要的正文頁都由內建模型建立 512 維頁向量，所有排名候選均恢復語意分數，且沒有呼叫外部模型或保存聊天。非 preview 審計再次確認，送往主生成歷史的是完整正文，不是小摘要；摘要只出現在提取助手的候選目錄。
- 另以完全虛構的故事實際呼叫已配置的總結及提取模型各一次：生成符合詳細長度的結構化目錄卡，測試指定的人物、地點、物件及期限詞全部保留；提取模型面對相關與無關兩頁只選相關頁，理由包含事件原因及物件持有人。此項驗證新提示詞與模型配合，不把虛構樣例冒充真實長聊天的召回結果；真實聊天舊摘要須明確重整後才會變成新格式。
- 182 項單元測試、清單／語法／模型雜湊檢查通過。`browser.py` 以真實 512 維內建模型完成摘要、選頁、完整正文取回及衝突接管，外部請求 0、瀏覽器錯誤 0；`missing_browser.py` 清空 IndexedDB 後不按按鈕即自動恢復 3/3 頁向量，摘要內容及 5 次既有合成 API 計數不變；`rebuild_browser.py` 的單頁、全部、停止、失敗保留與刷新不重跑均通過。

## 0.8.7：取用紀錄與真實回覆生命週期

- 修改前先加入 4 個回歸案例，全部穩定失敗：編輯／隱藏尚存回覆使紀錄消失、續寫同一楼層無法綁定、25 次無回覆請求擠掉成功紀錄、晚到未綁定副本覆蓋已綁定紀錄。修正後 180 項單元測試通過；另覆蓋原生 swipe 型別、舊候選恢復、收到回覆前關自動不丟關聯、遺漏接收通知、空串流／切聊天防誤綁、聊天備份恢復、舊記錄遷移、重複識別的歧義及跨聊天保存隔離。多筆舊版失敗請求不會全部推定成同一個後來生成的回覆，也不能擠掉已確認綁定的紀錄。
- 回覆採用持久識別與 swipe 版本，不依賴可編輯正文／隱藏旗標的指紋。來源正文的失效檢查仍用指紋，未放寬檢索安全邊界。普通失敗請求只留在未完成工作狀態，不占已完成紀錄的 20 筆上限。
- 紀錄在收到角色回覆後同步放入聊天 metadata，再使用原生聊天保存及本機 IndexedDB 副本；刷新優先讀回聊天中的資料。沒有只讀紀錄卻重新跑模型的行為。舊版已失去關聯的快照有獨立折疊查看入口，不從舊樓號推定最近取用；快取已被清除且沒有聊天備份時不承諾恢復。
- `host_receipts.py` 在 LAN 原生頁面通過開發檔測試：真正呼叫酒館 `saveReply`（normal／continue／swipe），監聽實際的接收與渲染事件，使用原生 `saveChat` 產生包含 journal 和回覆識別的聊天資料。編輯／隱藏、切舊 swipe、23 次無回覆、刪最新回退、重載、新瀏覽器 context 無 IndexedDB 皆通過。後端聊天保存攔截至測試記憶體，真實帳號／聊天寫入 0、付費模型調用 0、瀏覽器錯誤 0。
- 真實頁面第一次重新生成探針其實停在 `chatId=null` 的歡迎頁；即使有真實模型回覆，該臨時會話原本就沒有聊天檔案可保存，因此不把那次刷新回滾誤報成 Folio 保存故障。其後先明確打開已有 `chatId` 的 4 則訊息聊天再點原生「重新生成」：捕獲到 `GENERATION_ENDED` 早於 `MESSAGE_RECEIVED`，新回覆有 Folio ID、聊天 metadata 1 筆、本機副本 1 筆、面板 1 筆，數次 `/api/chats/save` 均為 200；全新瀏覽器重開該聊天後四者仍可恢復。0.8.8 仍移除 Folio 在流式結束事件中額外發起的 `saveChat`，只同步暫存 metadata，交由緊接著的酒館原生回覆保存一起落盤，避免兩條保存鏈互相覆寫。
- 同一真實聊天的下一次重新生成以空角色樓層結束，舊收據仍在 metadata／本機副本，但空樓層沒有可綁定的成功收據；再下一次超過 180 秒未收到 `GENERATION_ENDED`。這兩次分別記為主生成空回與超時，沒有冒充 Folio 保存通過，也沒有繼續用模型調用重試。
- `usage_browser.py`、`lifecycle_browser.py`、`api_browser.py`、`api_switch_browser.py` 回歸通過，維持刪來源快照、任務取消與刷新、API 保存與來源隔離。不宣稱已測遍第三方插件組合或所有模型故障。

## 0.8.6：API 保存確認與刷新／關頁重開

- 修正 API 表單和模式開關只呼叫延遲保存卻立即顯示成功的問題。立即呼叫原生 `saveSettings` 後再從 `/api/settings/get` 核對模式及完整雙用途設定；原生保存函式吞掉 HTTP 錯誤也不會誤報成功。寫入失敗或核對不符，表單保留草稿、本視窗退回原設定；不宣稱讀回失敗時伺服器一定沒有寫入。
- 167 項單元測試通過（新增 8 項）。包含保存後換新 Host、不經模型列表直接生成、雙用途金鑰隔離、假成功、寫入例外、模式回退、保存中阻止半成品請求、等待保存時取消、未啟用設定保存及讀回失敗。
- `api_browser.py`、`api_switch_browser.py`、`tiered_browser.py` 回歸通過。現在明確等待保存結果，不再用點擊當下的記憶體狀態充當持久化驗收。七來源、保存模式、刷新、草稿／金鑰隔離、取消舊測試與三級設定不退化。
- `host_save.py` 在 LAN 原生酒館頁面通過開發檔驗收：實際使用原生保存函式，後端存取攔截為合成帳號，六次生成都是合成回覆。手填兩個模型 → 保存 → 測試 → 刷新 → 測試 → 拒絕保存 → 保留草稿並重試 → 關閉 context → 全新 context 重開 → 兩用途再測。全程 `/status` 回覆 503，不提供模型列表；未修改真實帳號／聊天，瀏覽器錯誤 0。
- `host_reload_live.py` 在修復部署前用當前已保存配置驗證：首次載入、刷新、關閉 context 後全新重開，各做一次總結／提取，六次真實短 API 調用全部通過，列表一律拒絕，帳號及聊天寫入攔截。因此尚未穩定重現使用者描述的「須再次取列表」全部成因，不把本次查到的假成功缺陷說成已證實的唯一原因。
- 已查看原生手機截圖；保存、失敗、重試沿用原有面板，不另加設定層級。測試結束關閉瀏覽器，未啟動常駐酒館或代理服務。

## 0.8.3：記憶助手的明確 API 模式開關

- 142 項單元測試通過（新增 5 項）。覆蓋保存未啟用的獨立配置不改模式／生效模型、切换保留金鑰、主模型動態變更、空模型／已刪連線提示、測試開始前及傳輸期間取消、晚到舊結果不覆蓋新測試。
- `api_switch_browser.py` 真實點擊／鍵盤操作：主 API ↔ 獨立 API，6 次合成請求逐一核對 model／endpoint／金鑰範圍。未保存草稿不啟用，主模式下保存獨立配置仍使用主模型；切換保留配置，刷新保持模式，舊測試延遲回覆丟棄，主聊天生成期間不允許切換，舊聊天整理請求 0、外部請求 0、瀏覽器錯誤 0。桌面與 390px 截圖已檢查。
- `tiered_browser.py` 和 `api_browser.py` 通過：三級設定、主模型跟隨、進階參數載荷、七種來源、模型列表、獨立金鑰與過期列表隔離維持正常。保存獨立配置不再隱式切換；原有調用方仍可明確要求配置後啟用。
- `host_usage.py` 的安裝版驗收增加 API switch 來回切換與生效狀態／模型列檢查，主聊天設定不變；所有宿主寫入和真實模型請求均攔截。測試使用隔離瀏覽器，沒有替用戶切換真實設定。

## 0.8.2：全部重做／僅補未整理與隱藏劇情

- 137 項單元測試通過（新增 15 項）。驗證僅補未整理保留有效／人工摘要、有效分段接續、正文／玩家背景失效、向量未載入不重做摘要、排隊範圍與取消、等待中已完成的競爭、任務刷新恢復及停止、隱藏劇情和玩家配對、原生系統通知／旁白區分、召回去重、刪除排除及原文／隱藏狀態不變。
- `missing_browser.py` 實際點擊兩個按鈕：3 頁中 2 頁待補（隱藏），只發 2 次合成摘要請求並保留第三頁人工摘要；全部重做另發 3 次。搜尋結果為空不縮小操作範圍，排隊取消、刷新不重跑、真實內建向量、桌面／390px 無橫向溢出皆通過，瀏覽器錯誤 0。初次測試服務漏設 MJS MIME，導致向量降級；已修正測試服務並重新驗證向量實際完成，未以放寬斷言略過。
- `rebuild_browser.py`、`lifecycle_browser.py`、`auto_progress_browser.py`、`usage_browser.py` 回歸通過。舊聊天自動請求 0；單頁／全部、保存與取消、晚回覆丟棄、刪除後取用紀錄回退不退化。全部使用合成故事／API，不產生付費請求。
- `host_scope.py` 使用 LAN 酒館的原生擴充發現、攔截器、`setOpenAIMessages` 及 `sendOpenAIRequest`，DOM 點擊兩種操作。1 頁待補只總結 1 次，全部重做另 2 次；原生序列化及送出邊界的 5 則合成消息中，選中的隱藏正文恰好 1 次。用戶聊天／設定寫入與模型請求均被攔截，其他第三方插件隔離，不宣稱覆蓋所有插件組合。`FOLIO_LOCAL=1` 可替換為本地開發檔；預設不替換，驗證已安裝檔案。
- 語法、manifest、內建模型及 runtime SHA-256 校驗通過。所有測試瀏覽器在 finally 關閉；沒有啟動額外酒館服務，也不會自動對真實聊天執行整理。

## 0.8.1：生成判斷與重整取消的完整狀態回歸

本次檢查限定在重整生命週期及相鄰資料安全，不能當作全插件無缺陷保證。先前 0.8.0 的正常「開始→結束」測試沒有覆蓋 dry-run、不成對事件和排隊取消；這是測試範圍遺漏。

- 122 項單元測試通過；新增 16 項，涵蓋 dry-run／quiet 不鎖住引擎、即時宿主狀態修復遺漏結束事件、晚到狀態查詢不覆蓋新生成、排隊取消不打斷主選頁、結束邊界取消不復活、保存中取消與快取回復、保存失败不留下可執行任務、慢摘要／向量取消、停止後不經自動隊列重跑、刪頁／切聊天、空目標及生成與建立任務競爭、停止保存失敗暫停而非繼續收費。
- `lifecycle_browser.py` 使用真正 index 事件註冊、DOM 控件、IndexedDB、內建向量和合成 API。提示詞預計算後立即重整，3 頁恰好 3 次請求；排隊取消後不新增請求；遺漏結束事件後核對宿主空閒恢復 3 次；執行中停止第 7 次的晚到回覆被丟棄，保存中取消及刷新後仍為 7 次。取消不關閉自動開關、不停止主聊天，沒有外部付費請求、瀏覽器錯誤 0。
- `rebuild_browser.py`、`auto_progress_browser.py`、`tiered_browser.py`、`usage_browser.py` 回歸通過：單頁／全部重做、失敗恢復、舊聊天零自動請求、新回覆進度、三層設定與實際參數、刪除最新回覆回退舊取用、持久紀錄與來源刪除。不是僅檢查按鈕存在。
- `browser.py` 與 `api_browser.py` 回歸通過，維持真實內建 512 維向量推理、全文取用、七種接口設定、金鑰隔離及過期列表丟棄；本機測試沒有外部資源請求，所有助手回覆合成。
- 面板及浮層全程提供取消入口；保存時接受停止意圖並顯示停止中。排隊不再借用上次任務的滿進度條，取消排隊不冒充上次完成。停止狀態未能保存時只顯示「重試停止」，目前視窗不繼續發出重整請求；無法承諾儲存故障下的跨刷新持久取消。
- 局域網已原生更新至 0.8.1。`host_usage.py` 直接載入實機安裝檔案，向酒館真實事件匯流排發出 dry-run／quiet 事件，確認不進入等待；用原生生成控制核對 busy=true、結束後=false。實際點擊浮層「取消排隊」移除任務但保留主回覆生成狀態，三層設定、舊聊天零自動請求、3 次合成原生串流及刪除回退仍通過。付費請求 0、用戶聊天／主模型設定未改、瀏覽器錯誤 0；測試瀏覽器已關閉，未新增酒館服務。此實機測試隔離了其他第三方擴充，不宣稱所有插件組合均已覆蓋。

## 0.8.0：日常、個別設定、進階參數三層

- 新安裝預設兩用途跟隨酒館主 API；既有獨立接口與金鑰保留，可往返切換。不同 API 表單第二層、上下文／回覆長度／採樣／提示詞第三層；摘要詳略、重點、近期正文／舊頁召回是書頁目錄的第二層。所有複雜設定關閉面板後再次收起。
- 106 項單元測試通過，新增主模型动态切换、配置與金鑰保留、主 API／獨立／連線檔的實際參數、角色獨立設定、非法參數不覆寫、上下文超限和分段、近期回合完整配對、召回數／歷史預算實際生效，以及中途改設定不丟失分段正文。語法與內建模型／runtime 雜湊檢查通過。
- `tiered_browser.py` 真實點按三層、保存／刷新、設定摘要詳略與重點，手動整理三頁後切換主模型再重整一頁；4 次合成請求的模型、700-token 回覆上限、溫度、Top P、提示詞均正確。改設定及打開舊聊天沒有摘要請求，主聊天設定不變、瀏覽器錯誤 0。桌面／390px 截圖已檢查，無水平溢出。
- `browser.py`、`api_browser.py`、`usage_browser.py`、`rebuild_browser.py` 回歸通過：內建向量與全文取用、七種來源、金鑰隔離、模型列表不保存與過期列表丟棄，刪除最新回覆回退舊紀錄、來源刪除、重開持久化，生成中手動重整排隊、單頁／全部實際重做、失敗停止恢復。全部使用合成 API，不產生付費模型請求。
- 局域網部署與實機驗收已完成：先前酒館 8000 端口不可連，玩家啟動後透過原生擴充 updater 更新至 0.8.0。`host_usage.py` 直接載入已安裝原碼（未替換成本地模組），確認魔法棒入口、主 API 第一層、獨立接口第二層、參數第三層、重新開啟收起進階欄位，桌面與 390px 手機無水平溢出。
- 實機三次原生發送被攔截為合成串流（5／7／5 則），確認刪最新回覆回退現存舊紀錄、刪來源標示、IndexedDB 重開、歷史切換，以及手動重整排隊／浮層與新回覆進度。舊聊天自動請求 0，付費請求 0，聊天與主模型設定未改，瀏覽器錯誤 0。測試瀏覽器已關閉；沒有啟動額外酒館服務或修改遠端管理設定。

## 0.7.2：一鍵重整必定有回饋

- 「一鍵重新整理全部」不再因正文正在生成或空狀態而成為無說明的灰色控制。生成中點擊會保存排隊意圖、顯示「已排隊，等待回覆完成」，並在 `GENERATION_ENDED` / `GENERATION_STOPPED` 後建立手動任務；切換聊天會丟棄舊聊天的排隊意圖。
- 手動任務與自動新回覆共用右下工作票據，但標題、摘要進度與向量進度分開計算。票據會在模態面板開啟時移入 `<dialog>` 頂層，關閉時回到輸入區旁，避免被原生 top-layer 背景遮住。點擊包裝器完成後重新依引擎快照渲染，不能反向覆寫按鈕的 disabled 狀態。
- `engine.test.mjs` 驗證生成中排隊、生成結束後只建立一次任務並只呼叫一次總結。`rebuild_browser.py` 實際點擊按鈕，驗證排隊票據、桌面／390px 邊界、單頁與全部重整、失敗停止及沒有重複費用；`auto_progress_browser.py` 同時確認打開舊聊天仍為 0 次 API 呼叫，新回覆自動進度未退化。
- 局域網酒館已原生更新到 0.7.2。`host_usage.py` 直接載入伺服器已安裝原碼，在合成生成狀態下實際點擊一鍵按鈕，確認排隊狀態與票據可見；舊聊天自動請求 0、付費請求 0，三次主發送皆為攔截的合成內容，用戶聊天與主模型設定未改。

## 0.7.1：舊聊天只能由玩家啟動整理

- 打開或切換聊天時，引擎把當前所有書頁記為舊聊天邊界；「新回覆自動記憶」只收取之後的 `MESSAGE_RECEIVED` / `CHARACTER_MESSAGE_RENDERED` / swipe 回覆。單純開啟自動開關、刷新、切回聊天或定時 tick 都不能讓舊頁進入任務。關閉期間收到的回覆，重新開啟時也不補做。
- 99 項單元測試通過：新增「開啟兩頁舊聊天兩次 tick 仍為 0 次模型請求」、「舊＋新只整理新回覆」、「關閉期間回覆不補做」，以及「手動修改舊摘要可獨立建立向量」。一鍵／單頁手動重整、刪除移位與過期回覆保護繼續通過。
- `auto_progress_browser.py` 在真實 DOM 與 IndexedDB 開啟三頁舊聊天，等待後確認 API 請求 0、浮層隱藏、運行頁明示「舊聊天有 3 頁尚未整理」；後來新增第 4 頁才出現 0/1 新回覆進度。完成、503 重試、桌面／390px 戲態全通過，3 次合成回覆、瀏覽器錯誤 0。
- `browser.py`、`rebuild_browser.py`、`usage_browser.py` 回歸通過：前兩者先確認舊聊天零請求，再真實點擊一鍵重整；儀表測試確認單純開啟自動開關不會為三頁舊摘要建立後台任務。所有回覆合成，付費請求 0。
- 局域網酒館已原生更新到 0.7.1。`host_usage.py` 直接載入伺服器已安裝原碼：一頁開啟時已存在的未整理舊聊天經過主動 tick 後，摘要請求 0、記錄未寫入、`manualPending=1`；後續新回覆顯示獨立 0/1 進度浮層。原生魔法棒、三次合成串流、刪除回退與 IndexedDB 重開通過，用戶聊天／主模型設定未改，付費請求 0，瀏覽器錯誤 0。

## 0.7.0：看得見的自動整理

- 未整理正文觸發自動摘要後，主面板外持續顯示浮層：當前書頁、長文分段、摘要頁數、向量頁數與合計進度皆來自引擎快照。完成後短暫顯示再收起；失敗保留 503 錯誤、已完成數與「立即重試」。
- 95 項單元測試通過，新增未整理、生成等待、摘要／向量階段和全數完成狀態。`auto_progress_browser.py` 使用真實 DOM、IndexedDB 及內建向量模型，5 次合成 API 回覆、外部請求 0、瀏覽器錯誤 0。桌面、390px 手機、運行頁、完成和失敗戲態截圖已檢查，無水平溢出。
- 局域網酒館已原生更新到 0.7.0。`host_usage.py` 直接載入已安裝檔案，原生魔法棒、未整理第 3 頁的自動進度浮層、三次合成串流、刪除回退和 IndexedDB 重開皆通過；付費請求 0，用戶聊天／主模型設定未改，瀏覽器錯誤 0。

## 0.6.2：最近取用跟隨仍存在的回覆

- 每筆 request-ready 取用核對會在角色回覆進入聊天時绑定其内容指纹；只有对应回复仍存在的记录进入仪表与取用列表。连续生成两次后删除最新回复，界面回退至较旧但仍存在的回复记录；删除整组、刷新重开、来源楼层前移也按指纹判断，不按旧楼号猜。
- 94 項單元測試通過，新增「請求已組裝但回覆未出現」不搶占最近、正常回覆綁定、換回覆綁定、刪最新回覆回退及舊版緊鄰回覆安全推斷。原始快照仍按每聊天最多 20 筆保存，不会进入新的检索。
- `usage_browser.py` 使用真實 IndexedDB 和合成聊天完成两次请求／两次角色回复；删除第二次回复后选择器从两笔回到第一笔，并显示第一次玩家输入。随后未附带回复的新请求保持第一笔，附带第三次回复后才成为最近。付费请求 0、浏览器错误 0。
- `browser.py` 真實內建向量、摘要／提取及删除来源回归通过；新增的合成回复使用已完成摘要记录，未增加模型调用。总计 6 次合成助手请求、外部资源 0。
- 局域网酒馆原生更新到 0.6.2 后，`host_usage.py` 直接载入已安装文件并通过三次原生 `sendOpenAIRequest` 合成串流：第一次 5 则、第二次 7 则，删除第二个角色回复后回退第一笔；删除早期来源、重开 IndexedDB 后仍是第一笔；第三次 5 则生成后成为最新，并可切回旧笔。付费调用 0，用户聊天／主模型设置未改，浏览器错误 0，测试浏览器已关闭。

## 0.6.1：直觀的自動記憶開關

- 原生小勾選框改為滑動開關，開啟／關閉同時以滑塊位置、軌道顏色和「已開啟／已關閉」文字表示；整個標籤可點，語義為 `switch`，鍵盤空格和可見焦點通過瀏覽器測試。
- 92 項單元測試、語法／模型雜湊檢查及 `usage_browser.py` 通過。桌面與 390px 截圖已檢查，面板無水平溢出；開關沒有改變手動重整獨立於自動模式的原有行為。

## 0.6.0：圓形儀表與保留的發送紀錄

- 92 項單元測試通過。新增發送快照與暫存選頁隔離、刪最新回覆／來源後保留、來源移位／同內容碰撞、刷新與舊紀錄遷移、跨聊天慢載入隔離、20 筆上限／去重、保存失敗提示及下次恢復、重整不清掉快照；舊來源不參與新查詢。
- `usage_browser.py` 通過：真實 Chromium、IndexedDB、內建向量；圓環實際頁數、零頁「—」、沒有試跑輸入／按鈕、核對／未核對分區、刪最新回覆和來源、刷新持久保存、未送出的選頁不覆蓋、較早紀錄切換，兩個資料庫連線原子追加不丟紀錄。模型請求 0，瀏覽器錯誤 0。
- `browser.py`、`rebuild_browser.py`、`api_browser.py` 回歸通過：原有摘要／檢索、雙模型配置、單頁與一鍵重整功能維持正常。合成 API、真實本機推理；沒有為本次 UI 測試增加付費模型請求。
- 桌面與 390px 截圖已檢查：兩個圓環分別代表摘要／向量，最近發送摘要並排或手機下一行；來源刪除有明顯提示，原正文可展開，候選只留次要折疊區。手機無水平溢出。
- 局域網酒館已透過原生擴充更新載入 0.6.0。`host_usage.py` 直接使用已安裝檔案（沒有本地程式替換），原生魔法棒、`runGenerationInterceptors`、真正 `sendOpenAIRequest` 及 request-ready 核對通過：第一次 5 則歷史，刪來源後下一次 3 則；舊紀錄仍可切回、來源標示缺失，關閉再開資料庫可恢復。兩次後端回覆在隔離瀏覽器攔截為合成串流，付費呼叫 0；用戶原聊天和主模型設定完全不變，瀏覽器錯誤 0。實機桌面／390px 截圖已檢查，測試瀏覽器已關閉。
- `host_readonly.py` 亦再次通過原生過期請求阻擋，刪除後的已保存紀錄不會削弱發送前的安全核對。未重新啟動酒館，沒有新增常駐服務。
- 紀錄在酒館 request-ready 事件核對，不是服務商成功回執；目前只保留同瀏覽器最近 20 筆，不能恢復已被舊版刪除的紀錄。

## 0.5.0：真正執行的手動重整

- 82 項單元測試通過。新增自動關閉仍手動執行、指定末頁優先、整段恰好重做一次、重複點擊抑制、舊 revision 快取拒絕、刷新續做、失敗保留／停止恢復、已完成頁不回滾、排隊中刪除、等待舊慢請求退出、快取交易失敗、多視窗鎖及向量失敗不反覆收費。
- `rebuild_browser.py` 真實點按通過：先完成三頁，關閉自動後重做第三頁，確實只有該頁一次新模型呼叫；全段按鈕再產生三次呼叫，摘要及向量均 3/3，刷新無重複。503 時仍顯示舊摘要，停止後恢復原完成狀態。真實 IndexedDB／內建向量，模型回覆為合成；桌面／390px 無溢出、瀏覽器錯誤 0。
- `browser.py` 既有摘要、檢索、刷新及雙模型介面回歸通過。
- `api_browser.py` 七來源 API 表單回歸與 `host_readonly.py` 原生刪除／過期請求攔截再次通過。
- `host_rebuild.py` 實機點按通過：載入酒館已安裝的 0.5.0，保留既有總結模型配置，用兩頁合成故事測試。自動記憶保持關閉，單頁按鈕產生一次真實摘要，全段按鈕再產生兩次；三次 DeepSeek Flash 均 HTTP 200 且正文非空，約 0.80–1.16 秒。兩頁摘要與向量完成，原聊天、主模型設定、合成正文均未改寫；瀏覽器錯誤 0，桌面／390px 實機截圖已檢查，瀏覽器已關閉。這不是對用戶所有真實長篇聊天的批量重整。

## 0.4.0：來源選擇與獨立 API

- 69 項單元測試通過，包括原有刪除一致性測試；新測網址整理、七種來源路由、主聊天金鑰隔離、同目的地才沿用已存金鑰、列表不保存表單、空模型、錯誤／HTML、取消中的晚回覆。
- `api_browser.py` 通過：七種來源網址預填、即時列表／手填選型、兩用途不同金鑰、保存後不回填明文、刷新仍獨立、換網址不帶走金鑰、401 不回顯服務商錯誤內容、過期列表丟棄；390px 無水平溢出、瀏覽器錯誤 0、外部請求 0。這項使用合成 API，不表示已持有七家的付費帳號。
- `browser.py` 和原生酒館 `host_readonly.py` 回歸通過：真實內建向量、刷新不重複摘要、兩模型 UI、刪除後真正 `sendOpenAIRequest` 在 fetch 前攔住過期歷史。沒有主聊天／設定寫入。
- `host_api.py` 實機通過：直接載入局域網酒館已安裝的 0.4.0（不是本地檔案替換），從原生魔法棒打開 API 表單，透過真正的酒館 `/status` 後端向 OpenRouter 匿名取得 447 個公開模型。網址整理、空 Authorization 和獨立 secret_id 確實經過後端；兩欄草稿互不影響，主模型設定不變，付費生成 0、瀏覽器錯誤 0。桌面與 390px 實機截圖已檢查，瀏覽器已關閉。
- 原生 Claude／Gemini 的欄位適配依酒館 1.18 原始碼核對；沒有宣稱用每家付費金鑰生成過。來源路由測試與公共模型列表驗收，不等同於用戶每一個模型的可用性驗證。

## 0.3.0：雙模型與刪除一致性

- 54 項自動測試通過。新增獨立模型／供應商路由、一次性設定遷移、空模型不呼叫、模型失敗不換主模型，以及中間配對刪除、單刪角色／玩家、慢摘要／提取期間刪除、有無事件通知、舊編輯視窗、樓層前移、重開舊快取、時間戳碰撞、刪光後新增等測試。
- 真實 IndexedDB 和桌面／390px 手機介面：兩欄填不同模型，保存、分別測試、刷新仍獨立；刪除第一個回合後，只剩兩頁，舊取用失效，未變動頁不重做摘要。
- 酒館 1.18 原生整合測試通過：`runGenerationInterceptors` 後刪除來源，再呼叫真正的 `sendOpenAIRequest`。過期內容在 JSON 序列化前被阻止，沒有第三次模型請求；只有先前兩次合成模型回覆。
- 原生事件會吞掉監聽器異常，因此不是只測「拋錯」。使用原生 stopGeneration 加上只針對該份過期請求的序列化阻擋，不替換全域 fetch。
- `tests/host_live.py` 雙模型真實 API 驗收通過：直接載入局域網酒館已安裝的 0.3.0，原生魔法棒可開啟面板。總結使用 `deepseek-flash`，提取使用 `deepseek/deepseek-v3.2`；三頁摘要、一次目錄提取、兩個模型各自的測試按鈕，共六次真實短請求均返回非空正文。此次提取約 17 秒；兩類請求上限均為 60 秒，刪除／切聊天仍可提前取消。
- 內建向量完成三頁索引，提取選回正確的藍色信件正文，七則歷史縮到五則，最新輸入保留。再刪除來源回合：舊取用紀錄清空、刪除頁的快取記錄移除、剩下兩頁指紋不變；真正的原生發送函式在 fetch 前攔住過期歷史，未增加模型請求。
- 雙模型配置僅用於隔離測試，未保存到用戶帳號。原聊天和主模型設定不變，應用寫入受攔截，瀏覽器錯誤 0；桌面和 390px 截圖已檢查，測試瀏覽器已關閉。未發送主模型正文生成，也未驗證所有其他插件並用情況。

## 0.2.0 歷史驗收

## 已通過

- `npm test`：36 項，含指紋、完整分段、混合檢索、去重、取消、保存恢復、多視窗鎖、故障備援、助手正文配對、v1 摘要遷移、未完成目錄保留歷史、重複全文核對、試跑不寫聊天，以及 DeepSeek 助手關閉思考／拒絕空回覆／獨立連線參數。
- `npm run check`：manifest、全部自有 JavaScript 語法、內建模型與 runtime SHA-256。
- `python tests/browser.py`：真實 Chromium、普通 HTTP、內建 ONNX / WASM。512 維、L2 norm 約 1；同義句 cosine 0.830，不相關句 0.431；這只是冒煙檢查，不是品質評測。首次推理約 4.9 秒，不代表手機性能。
- 三頁角色正文生成三次摘要，玩家輸入不獨立摘要；重開無重複摘要。魔法棒介面、搜尋、助手測試按鈕、暫停、Anima 接管及 390px 手機無水平溢出。外部資源請求 0、瀏覽器錯誤 0。此項助手 API 為合成回覆。
- `python tests/host_readonly.py`：已運行 SillyTavern 1.18.0 的實際 discovery、模型參數適配器、`runGenerationInterceptors`。選中正文及玩家背景進入歷史，最新輸入保留，主模型設定和原聊天不變；2 次助手 API 為合成回覆，應用寫入全部攔截。

## 真實助手連線驗收

`tests/host_live.py` 已通過：從局域網酒館直接載入已安裝的 0.2.0（沒有用本地模組冒充），使用既有 DeepSeek Flash 連線和合成故事。

- 三頁正文各完成一次摘要，三頁向量索引完成；再執行整理沒有重複保存。
- 真實助手從兩頁舊目錄候選中選回正確的藍色信件正文；七則歷史縮到五則，玩家背景與最後輸入仍保留。
- 四次短請求都有非空正文，`finish_reason=stop`，測試時約 1.14–1.50 秒；不代表實際長篇或所有網路的速度。
- 五則在原生 request-ready 事件的合成請求內找到完整內容。沒有發送主模型正文生成，這不等於對用戶所有預設的端到端驗證。
- 聊天與主模型設定保持不變。應用寫入被攔截；瀏覽器錯誤 0。桌面與 390px 手機截圖已檢查。

實機測試最初抓到 DeepSeek 回 200 但正文空白、`finish_reason=length`。修正助手請求的思考控制後才通過，不把 HTTP 200 當作摘要完成。DeepSeek 預設思考與關閉方式依 [官方文件](https://api-docs.deepseek.com/guides/thinking_mode/)；自訂後端欄位依 [SillyTavern 1.18 原始碼](https://github.com/SillyTavern/SillyTavern/blob/1.18.0/src/endpoints/backends/chat-completions.js)。只影響插件助手，不更改主聊天思考設定。

## 限制

沒有聲稱測過每個角色卡的 HTML、所有手機／酒館版本、多裝置同時編輯或數萬頁壓力。其他第三方擴充在隔離驗收瀏覽器不載入，未證明所有插件並用相容。

最終請求觀測驗證使用酒館真實事件搭配合成訊息；不發送真正的主模型正文回覆，也不宣稱驗證每個預設最後的全文。事件觀測不是供應商收件確認。所有瀏覽器均 finally 關閉，沒有啟動新酒館伺服器、端口或常駐服務。
