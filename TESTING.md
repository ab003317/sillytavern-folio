# 0.1.0 測試記錄

本記錄區分「真正跑過的部分」和「模擬的部分」，不把語法檢查當作可用性測試。

## 自動測試

`npm test`：25 項通過，涵蓋清理、全文拆段、SHA-256 與 Unicode、改文/swipe 指紋失效、JSON 輸出校驗、混合檢索、token 預算、內建分詞器、重開去重、完成記錄恢復、部分進度續做、切聊天/改文時慢請求取消、同瀏覽器交易鎖、向量故障不重跑摘要、不阻塞新頁整理、暫停、空白摘要拒絕、原文按序取用、保留最新輸入、合法空召回、API 故障備援、工具/多媒體歷史保護。

`npm run check`：manifest 入口存在、所有自有 JS/MJS 語法、模型完整 SHA-256。

## 真正的瀏覽器和向量模型

`python tests/browser.py`：Chromium，普通 HTTP、不具 secure context。直接使用倉庫內 ONNX 權重和 WASM runtime，不 mock 向量。

- 輸出 512 維，L2 norm 約 1。
- 合成語意檢查：「船長在港口碼頭等待來信」與同義句的 cosine 約 **0.830**，與晚飯牛肉麵句子約 **0.431**。僅是小型冒煙檢查，不是品質評測分數。
- 測試機首次推理含模型初始化約 **3.7 秒**；不代表所有手機或 CPU 的速度。
- 7 則合成正文各只生成一次摘要；重新載入頁面後沒有多做摘要。
- 桌面與 390px 手機版可搜尋、展開正文，沒有水平溢出。
- 小模型選擇不改原本聊天模型；歷史壓縮後保留最新玩家輸入。
- 停用後不再產生新摘要請求。外部資源請求 **0**，瀏覽器錯誤 **0**。

這個測試的摘要/選頁 API 使用合成回覆，沒有付費調用，也沒有用私人聊天做測試資料。

## 實際 SillyTavern 整合（唯讀演練）

`python tests/host_readonly.py`，對已運行的 SillyTavern **1.18.0**：

- 透過實際 `/api/extensions/discover` 流程載入 manifest 與模組，而不是只手工 import UI。
- 使用酒館實際 `createGenerationParameters` 適配器建立獨立短請求，測試 JSON 回覆解析。
- 使用實際 `runGenerationInterceptors`，證實回呼會被呼叫、選中原文進入歷史陣列、近期輸入保留。
- 原始聊天陣列、全域模型與預設參數不變。內建模型真實輸出 512 維。
- 2 個合成模型請求被測試攔截器回覆；**所有應用寫入與真正模型請求均被攔截**。
- 沒有在用戶伺服器安裝測試插件、寫入聊天、變更設定、停用 Anima 或重啟酒館。瀏覽器在 finally 關閉，沒有額外常駐服務。

## 尚未驗證／限制

- 沒有對用戶實際供應商計費 API 做端到端付費品質評估；路由測試不能保證每家代理都支援其列出的模型。
- 沒有聲稱已測過每個角色卡的自訂 HTML、所有酒館版本、所有手機瀏覽器、多裝置同時編輯、或數萬則聊天的壓力。
- 用戶其他會變更歷史的插件未與 Folio 同時啟用測試，需避免重複接管。
- 本插件交給酒館的歷史，不等於其他插件與酒館最後送出的完整 HTTP 請求；宿主仍有自己的容量裁剪。

因此這版標為 0.1.0；上述能力是已實作並驗證的起點，不是「永不遺忘」保證。
