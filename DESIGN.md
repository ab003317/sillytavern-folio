# Folio / 書頁

0.8 截圖檢視：桌面與 390px 第一層只保留正在使用的 API 方案、主 API 按鈕和關閉的「不同 API 與模型」，不露出網址、金鑰或長參數表。第二層維持雙用途對照，第三層以縮排及左側細線標示從屬关系；手機表單自然單欄且無水平溢出。書頁目錄的一鍵重整仍在最上方，摘要設定、單頁管理、較早取用紀錄與最近動作各自折疊。去掉面向開發者的 tomo 參考字樣，保留直接說明設定效果和舊聊天仍需手動重整的文案。

0.8 三層設定計畫：沿用海藍 #5aa9cc、海綠 #7bbdb1、墨藍 #253747、紙白 #eef4f7、灰藍 #8cabb9、琥珀 #d9af62；宋體標題與宿主正文字體、左對齊不變。主畫面保留工作與閱讀，設定採原生 details，預設關閉，每次重新開啟面板收起進階層。

版面：`API：跟隨酒館主 API [使用] / ▸ 不同 API 與模型 [雙用途表單 / ▸ 進階參數與提示詞]`；`書頁：小摘要＋正文 / ▸ 摘要與取用設定 [摘要詳略、重點、近期正文、召回頁數 / ▸ 容量限制]`；`運行：圓環＋一鍵整理 / ▸ 最近動作`。單頁的釘選、修改、原文對照收進管理層，摘要與正文仍可直接閱讀。

審查：tomo 參考的是「跟隨主 API」以及近期正文、召回數量與预算的分層設定；Folio 仍以摘要選取完整正文，不引入另一套大總結注入機制。預設主 API 要動態跟隨酒館模型切換，不能僅複製當前模型名稱。既有獨立 API 保留並明示，可一鍵切到主 API且可恢復原設定。進階欄位必須影響實際請求；折疊只是隱藏操作複雜度。

0.7.2 按鈕回饋修正：沿用海藍 #5aa9cc、海綠 #7bbdb1、墨藍 #253747、灰藍 #8cabb9、琥珀 #d9af62；標題仍用系統宋體，控制與數字沿用酒館字體。手動重整不再因正文正在生成或目前沒有可見書頁而成為沒有解釋的灰色按鈕；點擊後若正在生成，先明確排隊並在生成結束後開始。唯一醒目的動態元素仍是既有的右下工作票據，不新增卡片或裝飾。

版面草圖：`[一鍵重新整理全部] → [右下：等待本次回覆完成／正在建立任務／摘要 x/N・向量 y/N] → [短暫完成回執]`。面板內同時保留同一組真實數字；手動與自動工作共用位置但使用不同標題，避免兩個浮層互相覆蓋。按鈕只有在任務確實已接受或正在寫入任務時才鎖定，並把文字改成狀態而不是維持原動詞。

設計審查：一般 toast 只顯示「已點擊」仍會讓長任務像沒有工作；本修正把回饋綁定到引擎的排隊、摘要與向量狀態。也不在生成中強行改聊天資料，而是接受操作後安全排隊。空聊天、缺少模型或衝突仍讓按鈕可點，因為可操作的明確錯誤比無理由 disabled 更容易修正。舊聊天仍絕不自動處理；只有這次玩家點擊建立手動任務。

截圖檢視：桌面工作票據原本位於模態 `<dialog>` 之外，實際會被瀏覽器 top layer 的背景壓暗；現改為面板開啟時移入 dialog、關閉時回到 body。桌面排隊票據標題、原因、進度軌與「查看進度」在 350px 內一眼可讀；390px 手機版左右各留 12px，沒有水平溢出，並仍露出「舊聊天等待手動整理」的原始狀態，能分清這是玩家剛按下的任務而非自動掃描。沒有再加第二個確認框或裝飾動畫。

0.7.1 語義修正：「自動記憶」只代表開啟後新生成的角色回覆會自動寫入目錄，不是掃描並補做既有聊天。打開舊聊天時建立一條「現在」邊界：邊界前無論尚未摘要或尚未建立向量，都只顯示「舊聊天未整理」，絕不發出模型請求；只有玩家按「一鍵重新整理全部」才跨過這條邊界。

介面計畫：保留海藍 #5aa9cc、海綠 #7bbdb1、墨藍 #253747、灰藍 #8cabb9 與琥珀 #d9af62，不新增一組視覺。運行頁的自動區改成三個明確狀態：`[等待新回覆｜只整理接下來的回覆]`、`[舊聊天有 N 頁未整理｜請按下方一鍵重整]`、`[新回覆整理中｜真實進度]`。只有第三種顯示輸入區浮層與進度條；舊聊天不用彈窗催促，操作入口仍是已有的藍色「一鍵重新整理全部」。左對齊、標題系統宋體、正文宿主字體不變。

審查：「自動開關 + 現有未完成數」很容易被實作成後台全庫掃描，這不符合故事書頁的心智模型。修正後「自動」的邊界是事件（現在以後的新回覆），「重新整理」的邊界才是範圍（整個聊天）。不新增「掃描舊頁」的第二個開關，因為玩家已經有一個語義清楚的手動按鈕。

實機截圖檢討：舊聊天時頁首開關直接改名「新回覆自動記憶」，運行區第一行寫「舊聊天等待手動整理」，緊接著在同一視線的藍色一鍵按鈕；不顯示空的線性進度條，也不在輸入區出現浮層。新第 4 頁出現後，浮層文案是「正在整理新回覆」且只顯示這批的 0/1，不把舊三頁混進分母。桌面資訊密度足夠，390px 浮層不溢出；內容邊界比額外裝飾更醒目，因此沒有再加警告色卡片或確認彈窗。

0.7 計畫：自動整理需要在主面板關閉時仍看得見。沿用海藍 #5aa9cc 作摘要進度、海綠 #7bbdb1 作向量進度、墨藍 #253747／宿主底色作浮層、灰藍 #8cabb9 作軌道、琥珀 #d9af62 作等待重試；標題仍用系統宋體，狀態與數字沿用宿主字體和 tabular numerals。

布局：桌面在輸入區上方右側放一個窄的工作票据：`[書頁圖示  自動整理中] [查看] / [第 N 頁・第 x/y 段] / [━━━━進度]`；手機寬度為視窗減 24px，不覆蓋輸入欄。運行頁在圓形總覽之前放同一份常駐細進度條，避免浮層和面板說不同數字。失败时进度不消失，改显错误与“立即重试”；完成只短暂出现一次后自动收起。

審查：一般 SaaS toast 只說「處理中」且很快消失，對長篇摘要沒有用。本提示要顯示真實摘要頁數／向量頁數、當前頁和長文分段，直接由引擎狀態計算，不做假百分比或无限动画。浮层不使用模态对话框、不抢输入焦点、不逐页反复弹出；同一批工作从发现待整理到全部向量完成只占一个位置。手动重新整理已有独立进度，不与自动浮层混合。

實機截圖檢討：桌面浮層 350px 寬可以一眼讀完「頁／分段／摘要／向量」，手機 390px 時左右留 12px 且不壓住頁面正文；運行頁的同步狀態位於兩個圓環之前，視覺層級正確。原生 progress 在宿主主題下對比不穩定，因此改成明確的灰藍軌道／海藍進度／失敗琥珀；進度以「摘要完成 + 向量完成」為分子、兩倍書頁為分母，確保由 0 單調前進到 100%，不在階段切換時倒退。失敗截圖中錯誤、已完成數和「立即重試」同時可見，不會讓用戶猜它是否還在工作。

0.6.2 語義修訂：「最近取用」不是最近一次 request-ready 事件，而是目前聊天中仍存在的最近角色回覆所對應的取用。請求核對後先保存快照但不占据仪表；角色回复出现后绑定内容指纹并显示。删除最新回复立即回退到上一笔现存回复；删除后又恢复同一内容可重新识别。来源正文是否还在与结果回复是否还在分开标记，历史快照不参与新检索。

0.6.1 計畫：把頁首原生小勾選框改為明確的滑動開關，沿用海綠 #7bbdb1 表示運行、灰藍 #8cabb9 表示關閉，旁邊固定顯示「自動記憶」及動態文字「已開啟／已關閉」。布局為 `[軌道●] [自動記憶 / 狀態]`，整塊標籤可點、觸控高度至少 44px；手機仍放在標題下方，不搶圓形儀表的主視覺。宿主字體、左對齊，不新增卡片或動畫裝飾。

審查：單靠軌道顏色對色弱使用者不夠清楚，因此状态必须同时以文字呈现；隐藏原生 input 但保留 `role=switch`、键盘切换、屏幕阅读器名称和独立聚焦框。只让滑块对用户操作产生短距离移动，并遵守 reduced-motion；不把自动重整和手动重整混成同一个开关。

0.6.1 截圖檢視：桌面開關與關閉按鈕同列但有清楚間距，手機移到標題下方；48×27px 軌道、44px 可點區和雙行文字沒有擠壓分頁。開啟時海綠軌道與右側滑塊呼應向量圓環，同时显示「已开启」；关闭状态的自动化断言确认文字与 checked 状态同步。圆环仍是页面主视觉，开关没有被做成第二张卡片。

0.6 計畫：應用使用者指定的圓形儀表，主色海藍 #5aa9cc、索引海綠 #7bbdb1、紙白 #eef4f7、墨藍 #253747、灰藍 #8cabb9、警示琥珀 #d9af62；背景沿用酒館主題。宋體用於書頁感標題，內文用宿主字體、數字採等寬數字特性。左對齊標題，只有圓環中央的數字居中。

布局：`[摘要完成圓環] [向量就緒圓環] | [最近發送摘要／查看紀錄]`，其下放原有全段重整操作和精簡動作時間線；手機兩個圓環並排，最近發送摘要移到下一行。圓環表示完成頁／總正文頁，零頁顯示「—」而非假裝 100%。不做沒有意義的第三個裝飾圓環。

審查：使用者想看運行狀態與已發送內容，不要模擬操作。移除取用頁所有試跑欄位／按鈕；取用頁先呈現最新發送快照的正文及玩家背景，較早紀錄用一個選單切換，候選與未核對内容預設折疊。已發送快照獨立於暫存選頁，刪對話／重整不得抹去；來源缺失標成「已刪除或變更」，不拿舊樓號重新解析。持久紀錄只供查看，不能進入檢索或重新注入。

0.6 截圖檢視：圓環成為唯一醒目的視覺元素，去掉舊五段流程列表，沒有增加裝飾卡片。桌面把最近取用與圓環並排，手機保持雙圓環後接發送摘要；正文快照展示短節錄，全文可展開。刪除提示與「來源已刪除或變更」採琥珀色，原頁數標「當時」，不提供會誤跳現樓層的連結。重整按鈕保留在儀表下方；390px 可正常捲動、無水平溢出。

0.5 計畫：沿用海藍 #397b9d、紙白 #eef4f7、墨藍 #253747、灰藍 #8cabb9、琥珀 #d9af62，宋體標題、酒館內文字體。魔法棒面板「運行」与「書頁目錄」頂端都放 `[一鍵重新整理全部] [重整進度／停止]`；閱讀區的單頁按鈕移到正文前方，點後直接顯示排隊／處理／完成。左對齊、手機按鈕可換行。

審查：不能只加一個清空按鈕。手動重整不依賴自動開關，指定頁優先；全段重做要先在面板內說明頁數、費用及人工摘要會被新結果取代。舊摘要在新摘要成功前保留為可恢復備份，取消不回滾已完成頁，不刪正文。使用清楚的頁數進度，不新增裝飾卡片。

0.5 截圖檢視：手機「一鍵重新整理全部」及完成進度在面板首屏可見；單頁按鈕已移到正文之前，等待期間保持選中頁與舊摘要，旁邊能看到正處理的頁／段。沒有依靠只出現一下的通知來表示工作完成。

0.4 計畫（參考 tomo 的 API 表單操作，不複製參考專案程式碼）：沿用海藍 #397b9d、紙白 #eef4f7、墨藍 #253747、灰藍 #8cabb9、琥珀 #d9af62；宋體標題、酒館內文字體。兩個用途仍並排、手機上下排列，全部左對齊。每欄按「來源 → 網址 → 金鑰 → 模型 → 保存／測試」排列；既有酒館連線也是一種來源，不強迫重填。

版面草圖：`[總結：來源／網址／金鑰／模型] | [提取：來源／網址／金鑰／模型]`。取得模型列表是模型欄旁的次要操作，實際測試有獨立結果；列表成功不等於模型能用。金鑰遮罩、已保存提示、不回填明文；換來源或網址不自動帶走原金鑰。欄位有可點擊說明，生成參數不露出。

審查：本次不是新增通用 API 儀表板，不加入 tomo 的多檔案管理、輪換金鑰或參數滑桿。保留「寫目錄／選正文」兩個工作用途，錯誤顯示在對應欄位。酒館原生後端負責跨域，插件不開代理服務、不修改全域連線或主聊天金鑰。

0.4 截圖檢視：兩欄來源、網址、金鑰、模型排列對齊；390px 改成單欄，表單可捲動且無水平溢出。金鑰明文不出現在截圖或已保存狀態，保密提醒改為欄位下常駐小字，不只藏在說明中。

0.3 截圖檢視後修訂：電腦版兩個模型區塊改為並排，兩者可同時看見；手機上下排列。減少電腦版不必要的捲動，區段仍使用語意分隔線，不加裝飾卡片。

0.3 計畫：魔法棒保留四個分頁；「記憶助手」改為上下兩個具名區段：總結模型（寫小摘要）、提取模型（讀小摘要選正文）。各自有現有連線、模型、保存、測試，不共用隱藏選擇或失敗後改叫主模型。舊設定只在首次遷移時複製到兩欄。沿用海藍 #397b9d、紙白 #eef4f7、墨藍 #253747、灰藍 #8cabb9、琥珀 #d9af62、酒館內文字體及宋體標題，左對齊，手機單欄。

審查：兩區對應實際兩項工作，不做兩個裝飾性設定卡。模型名稱與實測用途都要對得上。刪除後的舊取用紀錄失效，而非保留過期樓號；管理操作以訊息指紋和原物件核對，不能在樓號移動後作用於另一頁。檢查普通刪除、中間／末尾／玩家輸入刪除、慢摘要／慢提取期間刪除、快取重開、相同時間戳碰撞與手機畫面。

目的：聊天是一本書，正文是書頁，摘要是目錄。目錄用於挑頁，不取代被選中的正文。

視覺計畫：海藍 #397b9d、紙白 #eef4f7、墨藍 #253747、灰藍 #8cabb9、提示琥珀 #d9af62；實際底色、文字沿用酒館主題。標題採系統宋體，正文沿用酒館字體。左對齊的單欄目錄，頁碼是實際訊息序號，不是裝飾性編號。手機同一結構。

0.2 修訂：入口在輸入欄魔法棒，不在擴充設定卡片。主面板有「運行」「書頁目錄」「本次取用」「記憶助手」四頁；資訊完整、操作少，技術參數不出現在介面。

正文單位是一則角色的故事回覆；前置玩家輸入是這頁的背景，不獨立產生一張書頁。目錄摘要與正文對照。近期回合以完整配對保留；向量與小模型只從完成的摘要選舊正文，不能以正文截取冒充摘要。取用頁分清候選、模型選中、容量略過、實際請求觀測結果。

版面計畫：既有海藍/紙白/墨藍/灰藍/琥珀色保持不變，沿用酒館主題。標題系統宋體、正文酒館字體。桌面「頁目錄 | 閱讀區」雙欄，手機切成單欄清單展開。運行頁用帶文字說明的流程列表，不用不明意義的大數字卡片。助手頁只選既有連線/模型，不提供網址/金鑰/向量參數表單。

審查：上一版縮得太少，既隱藏工作過程，又把玩家和正文混成同等書頁；這次詳細呈現資料流，不靠增加一堆設定來假裝功能完整。所有說明按鈕可觸控/鍵盤，不藏在 hover。首要驗收是局域網已安裝實例可直接開面板並完成真實小模型請求。

檢討：不做一般儀表板的大數字卡片、不暴露模型/維度/top-k/批次/樓層門檻。取用頁直接顯示實際正文來源與原因，避免把同一批的多個事件誤看成重複解析。

技術邊界：獨立前端擴充，標準 manifest + generate_interceptor；無後端插件、無常駐服務。真正打包中文 BGE int8 模型和 ONNX Web runtime；無 CDN、無 Hugging Face 運行期請求。向量在 worker 內算；0.3 起摘要與目錄選擇使用各自保存的連線和模型發送獨立短請求，缺少設定或呼叫失敗時不暗中換模型。絕不修改全域模型/預設。

安全：原始聊天不刪不隱藏。內容指紋核對，逐段保存摘要，切換聊天/改文/換 swipe 取消舊工作。只改本次 coreChat 的歷史陣列，不往系統提示塞世界書。錯誤回退並可見，摘要請求有取消/時限，worker 有時限/終止；測試不啟動酒館服務。
